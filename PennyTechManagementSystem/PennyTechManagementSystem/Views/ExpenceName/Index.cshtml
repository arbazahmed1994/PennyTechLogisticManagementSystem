@model IEnumerable<PennyTechManagementSystem.Models.ExpenceNameModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_NestedLayout.cshtml";
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table-custom table table-bordered">
    <thead>
        <tr>
            
            <th>
                @Html.DisplayNameFor(model => model.ExpenceName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExpenceCategoryName)
            </th>
            <th colspan="2">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ExpenceName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ExpenceCategoryName)
                </td>
                <td>
                    <a href="@Url.Action("Edit", "ExpenceName", new { id=item.ExpenceNameID })"><i class="fa fa-pencil"></i></a>
                </td>
                <td>
                    <a href="@Url.Action("Delete", "ExpenceName", new { id=item.ExpenceNameID })" onclick="return confirm('Do you really want to delete this item?')"><i class="fa fa-trash"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>
